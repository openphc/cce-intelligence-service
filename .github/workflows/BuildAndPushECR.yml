name: Java CI and GHCR Push

on:
  push:
    branches: [ "devops/uhis-958" ]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

env:
  IMAGE_URI: ghcr.io/${{ github.repository }}
  IMAGE_TAG: ${{ github.sha }}

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v5

    - name: Set up JDK 21
      uses: actions/setup-java@v5
      with:
        java-version: '21'
        distribution: 'temurin'

    # Configure Gradle for optimal use in GitHub Actions, including caching of downloaded dependencies.
    # See: https://github.com/gradle/actions/blob/demo/setup-gradle/README.md
    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v4

    - name: Build with Gradle
      run: ./gradlew build

    - name: Build container image
      run: ./gradlew bootBuildImage --imageName=${IMAGE_URI}:latest

    - name: Log in to GitHub Container Registry
      uses: docker/login-action@v3
      with:
        registry: ghcr.io
        username: ${{ github.repository_owner }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Tag & Push Image
      run: |
        docker tag ${IMAGE_URI}:latest ${IMAGE_URI}:${IMAGE_TAG}
        docker push ${IMAGE_URI}:${IMAGE_TAG}
        docker push ${IMAGE_URI}:latest

  # trigger-deploy:
  #   needs: build
  #   runs-on: ubuntu-latest
  #   if: github.event_name == 'push' && github.ref == 'refs/heads/demo' && success() # only for push to demo and docker image is successfully created
  #   env:
  #     GH_TOKEN: ${{ secrets.CROSS_REPO_TOKEN }}
  #   steps:
  #     - name: Trigger deploy-scripts workflow_dispatch
  #       run: |
  #         gh api --method POST \
  #           /repos/openphc/deploy-scripts/actions/workflows/deploy.yml/dispatches \
  #           --input - <<'JSON'
  #         {
  #           "ref": "main",
  #           "inputs": { "target_env": "dev" }
  #         }
  #         JSON
